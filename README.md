# Monotonicity constraints in neural networks

This repository provides implementations and benchmarks for studying monotonicity constraints in neural networks using Python, NumPy, and PyTorch. Such constraints are important in applications where predictions should preserve known monotonic relationships with respect to selected input features, especially in finance, healthcare, and safety-critical decision systems.


